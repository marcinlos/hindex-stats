[tool.poetry]
name = "hindex-stats"
version = "0.1.0"
description = "Automatic Google scholar data fetching for multiple authors"
authors = ["Marcin Łoś <marcin.los.91@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.8"
scholarly = "^1.7.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
